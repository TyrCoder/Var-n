# Order Management System - Visual Architecture

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                         CUSTOMER JOURNEY                            │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│   Browse     │      │  Add to Cart │      │   Checkout   │
│  Products    ├─────→│              ├─────→│              │
└──────────────┘      └──────────────┘      └──────────────┘
                                                     │
                                                     ↓
                                            ┌──────────────────┐
                                            │  Place Order API │
                                            │  /api/place-order│
                                            └────────┬─────────┘
                                                     │
                    ┌────────────────────────────────┼────────────────────────────────┐
                    │                                │                                │
                    ↓                                ↓                                ↓
            ┌──────────────┐          ┌──────────────────┐          ┌──────────────┐
            │Create Order  │          │Create Order      │          │Create Shipment
            │(PENDING)     │          │Items             │          │Record
            └──────────────┘          └──────────────────┘          └──────────────┘
                    │
                    ↓
            ┌─────────────────────────────────────┐
            │  Order Confirmation Page            │
            │  - Show order number                │
            │  - Show progress tracker            │
            │  - Start polling every 30 seconds   │
            └────────────────┬────────────────────┘
                             │
                             ↓
            ┌────────────────────────────────────┐
            │  GET /api/order-status/{order_id}  │ (Every 30 seconds)
            │  Returns: Current order_status     │
            └────────────────┬───────────────────┘
                             │
                             ↓
                    ┌────────────────┐
                    │ Progress Bar   │
                    │ Updates        │
                    │ Automatically  │
                    └────────────────┘
```

---

## Seller Order Management Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                      SELLER ORDER MANAGEMENT                        │
└─────────────────────────────────────────────────────────────────────┘

                    ┌──────────────────────────┐
                    │  Seller Dashboard        │
                    │  /seller-dashboard       │
                    └───────────┬──────────────┘
                                │
                                ↓
                        ┌───────────────┐
                        │  Orders Page  │
                        │  Sidebar Link │
                        └───────┬───────┘
                                │
                ┌───────────────┼───────────────┐
                ↓               ↓               ↓
        ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
        │Filter: All   │ │Filter:       │ │Filter:       │
        │              │ │Pending (⏳)  │ │Confirmed ✔️  │
        └──────────────┘ └──────────────┘ └──────────────┘

                    ┌─────────────────────────┐
                    │  Order List Display     │
                    │  - Order #              │
                    │  - Customer Name        │
                    │  - Total Amount         │
                    │  - Status Badge         │
                    │  - [View] [Update]      │
                    └────────────┬────────────┘
                                 │
                                 ↓
                    ┌─────────────────────────┐
                    │  Click [Update] Button  │
                    │  Opens Modal            │
                    └────────────┬────────────┘
                                 │
                                 ↓
                    ┌─────────────────────────┐
                    │  Status Dropdown        │
                    │  - pending              │
                    │  - confirmed ← select   │
                    │  - processing           │
                    │  - shipped              │
                    │  - delivered            │
                    └────────────┬────────────┘
                                 │
                                 ↓
                    ┌─────────────────────────┐
                    │  POST Request Sent      │
                    │  /seller/update-status  │
                    │  order_id=X             │
                    │  new_status=confirmed   │
                    └────────────┬────────────┘
                                 │
                    ┌────────────┴────────────┐
                    ↓                         ↓
            ┌──────────────┐        ┌──────────────┐
            │ Validate:    │        │Update DB:    │
            │ - Seller own │        │UPDATE orders │
            │   product    │        │SET status... │
            │ - Valid enum │        └──────────────┘
            └──────────────┘
                    │
                    ↓
            ┌──────────────────────┐
            │ Success Response     │
            │ Order status changed │
            └──────────────────────┘
```

---

## Real-Time Update Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                    REAL-TIME SYNCHRONIZATION                        │
└─────────────────────────────────────────────────────────────────────┘

TIME: T₀
┌───────────────────────────────────────────────────────────────────┐
│ SELLER                          │          BUYER                   │
├───────────────────────────────────────────────────────────────────┤
│                                │                                   │
│  [Update] ────order_status──→  │                                   │
│                                │          Orders Table             │
│                                │    order_status: 'confirmed'      │
│                                │                                   │
└───────────────────────────────────────────────────────────────────┘

TIME: T₀ + 30 seconds
┌───────────────────────────────────────────────────────────────────┐
│ SELLER                          │          BUYER                   │
├───────────────────────────────────────────────────────────────────┤
│                                │                                   │
│  (Dashboard refreshed)         │  JavaScript Polling Triggers:     │
│  Shows "Confirmed" ✔️          │  fetch(/api/order-status/X)      │
│                                │         ↓                         │
│                                │  Returns: status='confirmed'      │
│                                │         ↓                         │
│                                │  Progress Bar Updates:            │
│                                │  ⏳ ─→ ✔️ (highlighted)           │
│                                │                                   │
│                                │  Status Message Updates:          │
│                                │  "✔️ Seller confirmed order!"    │
└───────────────────────────────────────────────────────────────────┘
```

---

## Database Schema Relationship

```
┌─────────────────────────────────────────────────────────────────┐
│                        DATABASE TABLES                          │
└─────────────────────────────────────────────────────────────────┘

users                 orders                    order_items
─────                 ──────                    ───────────
id ─────────→ user_id ─→ order_id ────→ order_id → product_id
first_name    seller_id   order_number  quantity
last_name     order_status created_at   unit_price
email         updated_at   total_amount size
role          payment_method            color
              
              sellers                   products
              ───────                   ────────
              seller_id ←─ orders.seller_id ─ seller_id
              store_name               id
              status                   name
              rating                   price
                                       seller_id
```

---

## API Call Sequence Diagram

```
TIME SEQUENCE

t=0    Customer Place Order
       ├─→ POST /api/place-order
       │   ├─ Validate cart
       │   ├─ Create order (status='pending')
       │   ├─ Create order_items
       │   ├─ Create shipment
       │   └─ Return: {success, order_id, order_number}
       │
       └─ Redirect to /order-confirmation/{order_number}

t=1    Page Loads with Progress Tracker
       ├─ Show: ⏳ PENDING status
       ├─ Start: setInterval(updateOrderStatus, 30000)
       └─ Display: "Waiting for seller..."

t=5    Seller Updates Order
       └─ POST /seller/update-order-status
          ├─ Validate seller owns order
          ├─ Update: orders.order_status='confirmed'
          ├─ Update: orders.updated_at=NOW()
          └─ Return: {success: true}

t=31   Buyer Page Polls (30 seconds later)
       ├─ GET /api/order-status/{order_id}
       ├─ Receives: {status: 'confirmed', ...}
       ├─ Progress Bar Updates: ⏳ → ✔️ 
       └─ Message Updates: "✔️ Seller confirmed!"

t=61   Buyer Page Polls Again
       ├─ GET /api/order-status/{order_id}
       ├─ Receives: Same status (no change)
       └─ No visual change (keeps current state)

t=90   Seller Updates to "Processing"
       └─ POST /seller/update-order-status
          ├─ Update: orders.order_status='processing'
          └─ Return: {success: true}

t=121  Buyer Page Polls Again
       ├─ GET /api/order-status/{order_id}
       ├─ Receives: {status: 'processing', ...}
       ├─ Progress Bar Updates: ✔️ → 🔄
       └─ Message Updates: "🔄 Order being prepared..."
```

---

## Status Progression State Machine

```
                   ┌─────────────────────────────────────┐
                   │        ORDER STATUS FLOW             │
                   └─────────────────────────────────────┘

                         START
                          │
                          ↓
                    ┌─────────────┐
                    │   PENDING   │ ⏳
                    │ (Order New) │
                    └──────┬──────┘
                           │ (Seller confirms)
                           ↓
                    ┌─────────────┐
                    │ CONFIRMED   │ ✔️
                    │(Processing) │
                    └──────┬──────┘
                           │ (Seller packing)
                           ↓
                    ┌─────────────┐
                    │ PROCESSING  │ 🔄
                    │ (Preparing) │
                    └──────┬──────┘
                           │ (Seller ships)
                           ↓
                    ┌─────────────┐
                    │   SHIPPED   │ 📦
                    │   (Transit) │
                    └──────┬──────┘
                           │ (Auto or manual)
                           ↓
                    ┌─────────────┐
                    │ DELIVERED   │ ✅
                    │  (Complete) │
                    └─────────────┘

                    ALTERNATE FLOWS:

        PENDING → CANCELLED ❌ (from any state)
        SHIPPED → RETURNED ↩️ (customer/seller initiated)
```

---

## Security Flow

```
REQUEST VALIDATION CHAIN

POST /seller/update-order-status
        │
        ↓
    ┌─────────────────────────────┐
    │ 1. Check Session             │
    │ Is user logged in?           │
    │ ✓ YES → Continue             │
    │ ✗ NO → Return 401            │
    └─────────────┬───────────────┘
                  │
                  ↓
    ┌─────────────────────────────┐
    │ 2. Get Request Parameters    │
    │ order_id, new_status         │
    │ ✓ Valid → Continue           │
    │ ✗ Missing → Return 400       │
    └─────────────┬───────────────┘
                  │
                  ↓
    ┌─────────────────────────────┐
    │ 3. Validate Status Enum      │
    │ Must be in: [pending,        │
    │ confirmed, processing,       │
    │ shipped, delivered,          │
    │ cancelled, returned]         │
    │ ✓ Valid → Continue           │
    │ ✗ Invalid → Return 400       │
    └─────────────┬───────────────┘
                  │
                  ↓
    ┌─────────────────────────────┐
    │ 4. Verify Seller Ownership   │
    │ Does user_id match           │
    │ product.seller_id?           │
    │ ✓ YES → Continue             │
    │ ✗ NO → Return 403            │
    └─────────────┬───────────────┘
                  │
                  ↓
    ┌─────────────────────────────┐
    │ 5. Update Database           │
    │ UPDATE orders                │
    │ SET order_status = ...       │
    │     updated_at = NOW()       │
    │ WHERE id = ...               │
    │ ✓ Success → Return 200       │
    │ ✗ Error → Return 500         │
    └─────────────────────────────┘
```

---

## Frontend State Management

```
Order Confirmation Page State

┌─────────────────────────────────────────┐
│ JavaScript State Variables              │
├─────────────────────────────────────────┤
│ const orderNumber = '{{ order.order_no }}' 
│ const orderId = '{{ order.id }}'
│ let statusCheckInterval = setInterval() 
│
│ Data Structures:
│ {
│   order: {
│     id, order_number, status, 
│     status_label, status_emoji,
│     progress_step, created_at,
│     total_amount, customer_name
│   },
│   items: [{product_name, qty, ...}],
│   timeline: {
│     pending: {label, emoji, step},
│     confirmed: {...},
│     ...
│   }
│ }
└─────────────────────────────────────────┘

        ↓

    Event Loop (Every 30 seconds)
    ├─ fetch(/api/order-status/{id})
    ├─ Parse response
    ├─ Update DOM elements:
    │  ├─ .progress-step-circle
    │  ├─ .progress-step-label
    │  ├─ .progress-connector
    │  └─ .order-status-message
    └─ Re-render progress bar HTML
```

---

## File Structure

```
Project Root
│
├── app.py
│   ├── GET /api/order-status/{order_id}    (NEW)
│   ├── GET /api/user-orders-detailed       (NEW)
│   └── POST /api/place-order               (existing)
│
├── templates/pages/
│   ├── checkout.html
│   ├── order_confirmation.html (UPDATED)
│   │   └── Progress tracker + polling
│   │
│   └── indexLoggedIn.html (UPDATED)
│       └── My Orders with status
│
└── static/
    ├── css/style.css
    └── js/script.js

Documentation:
├── ORDER_FLOW_GUIDE.md
├── ORDER_MANAGEMENT_SUMMARY.md
└── QUICK_START_TESTING.md (this)
```
